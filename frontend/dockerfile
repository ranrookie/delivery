# Step 1: Use Nginx base image
FROM nginx:alpine
# Step 2: Copy custom Nginx configuration file
COPY nginx.conf /etc/nginx/nginx.conf
# Step 3: Copy the built static files to Nginx's default directory
COPY html/ /usr/share/nginx/html/
# Expose port 80
EXPOSE 80